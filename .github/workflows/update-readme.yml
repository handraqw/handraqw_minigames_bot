name: Update README Multiple Times

on:
  schedule:
    # 30 раз в день с интервалом ~48 минут
    - cron: "*/48 * * * *"
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Generate random content and update README
        run: |
          # Создаем случайное число для разнообразия
          RANDOM_NUM=$(( RANDOM % 1000 ))
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')
          
          # Обновляем или создаем секцию в README
          if grep -q "<!-- AUTO-UPDATE -->" README.md; then
            # Заменяем существующий контент между маркерами
            sed -i '/<!-- AUTO-UPDATE -->/,/<!-- END-AUTO-UPDATE -->/d' README.md
          fi
          
          # Добавляем новый контент
          cat >> README.md << EOF

<!-- AUTO-UPDATE -->
## Auto-Generated Content

- **Last update:** $TIMESTAMP
- **Random value:** $RANDOM_NUM
- **Commit count:** $(date +%s)

This section is automatically updated 30 times per day.
<!-- END-AUTO-UPDATE -->
EOF

      - name: Commit and push changes
        run: |
          git add README.md
          git commit -m "Auto-update README: $(date '+%Y-%m-%d %H:%M:%S') - Random: $(( RANDOM % 1000 ))" || echo "No changes to commit"
          git push
